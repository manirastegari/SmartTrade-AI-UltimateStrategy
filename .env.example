# Copy this file to .env and fill in your values
# This file is committed (no secrets). The .env file is ignored by git.

# xAI Grok (required for AI overlay)
XAI_API_KEY=
# Optional: override model (defaults to grok-4-fast-reasoning)
XAI_MODEL=
# Optional: custom endpoint if needed
# XAI_BASE_URL=https://api.x.ai/v1

# Optional free API keys used by reliability fallbacks (safe defaults exist)
ALPHA_VANTAGE_API_KEY=
FINNHUB_API_KEY=
IEX_CLOUD_API_KEY=
POLYGON_API_KEY=
TWELVE_DATA_API_KEY=
